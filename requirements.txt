# ============================================================
# requirements.txt
# Professional Data Analyst / Engineer Project Dependencies
# 
# HOW TO USE:
#   1. Create a new folder for your project
#   2. Copy this file into it
#   3. Create a virtual environment: python -m venv venv
#   4. Activate it: venv\Scripts\activate (Windows)
#   5. Install everything: pip install -r requirements.txt
# ============================================================

# ── DATABASE CONNECTIONS ─────────────────────────────────────
sqlalchemy>=2.0.0          # database ORM and connection manager
psycopg2-binary>=2.9.0     # PostgreSQL adapter
pymysql>=1.0.0             # MySQL adapter (if needed)

# ── DATA MANIPULATION ────────────────────────────────────────
pandas>=2.0.0              # core data manipulation
numpy>=1.24.0              # numerical computing
pyarrow>=12.0.0            # fast data formats (parquet, feather)

# ── VISUALIZATION ────────────────────────────────────────────
matplotlib>=3.7.0          # base plotting library
seaborn>=0.12.0            # statistical visualizations
plotly>=5.14.0             # interactive charts

# ── EXCEL & REPORTING ────────────────────────────────────────
openpyxl>=3.1.0            # read/write Excel files
xlsxwriter>=3.1.0          # advanced Excel formatting
python-pptx>=0.6.21        # PowerPoint generation (optional)

# ── JUPYTER ──────────────────────────────────────────────────
jupyter>=1.0.0             # Jupyter notebook
ipykernel>=6.0.0           # Jupyter kernel for VS Code
nbformat>=5.0.0            # notebook file format

# ── DATA QUALITY & PROFILING ─────────────────────────────────
great-expectations>=0.17.0 # advanced data quality testing
ydata-profiling>=4.0.0     # automated EDA reports (optional)

# ── API & WEB ────────────────────────────────────────────────
requests>=2.28.0           # HTTP requests for APIs
httpx>=0.24.0              # async HTTP client
python-dotenv>=1.0.0       # load .env files for credentials

# ── DBT ──────────────────────────────────────────────────────
dbt-postgres>=1.5.0        # dbt for PostgreSQL
dbt-core>=1.5.0            # dbt core

# ── UTILITIES ────────────────────────────────────────────────
python-dateutil>=2.8.0     # date parsing utilities
tqdm>=4.65.0               # progress bars for long operations
loguru>=0.7.0              # better logging than print statements
pyyaml>=6.0.0              # read/write YAML files
